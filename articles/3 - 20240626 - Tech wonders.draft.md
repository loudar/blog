# Tech wonders

```js
{{md:execute-js}}

const span = create("span")
    .text("Hello world!")
    .build();

return span;
```

And the following is just another code block, but not executed:

```js
const span = create("span")
    .text("Hello world!")
    .build();

return span;
```
